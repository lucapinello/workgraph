description = "WorksGood profile — Claude CLI: opus standard/default worker, haiku for agency meta-tasks"

[agent]
model = "claude:opus"

[dispatcher]
model = "claude:opus"
max_agents = 8

[tiers]
fast = "claude:haiku"
standard = "claude:opus"
premium = "claude:opus"

[models.default]
model = "claude:opus"

[models.task_agent]
model = "claude:opus"

[models.evaluator]
model = "claude:haiku"

[models.assigner]
model = "claude:haiku"

[models.flip_inference]
model = "claude:haiku"

[models.flip_comparison]
model = "claude:haiku"
